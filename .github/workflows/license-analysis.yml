name: "CI"

on:
  pull_request:
    branches:
    - master

jobs:
  fileheader:
    runs-on: ubuntu-latest
    name: 'license file header'
    steps:
    - name: REUSE Compliance Check
      uses: fsfe/reuse-action@v1.1