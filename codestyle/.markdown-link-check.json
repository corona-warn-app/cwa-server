{
    "replacementPatterns": [
        {
            "pattern": "^/",
            "replacement": "/github/workspace/"
        }
    ],
    "ignorePatterns": [
        {
            "pattern": "^http[s]*://localhost.*"
        }
    ]
}
