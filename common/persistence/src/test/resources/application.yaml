---
spring:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: false
  main:
    banner-mode: off
logging:
  level:
    org:
      springframework: off
    root: off
