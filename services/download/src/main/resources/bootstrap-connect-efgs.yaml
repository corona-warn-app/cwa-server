---
spring:
  cloud:
    vault:
      kv:
        enabled: true
        backend: ${VAULT_BACKEND}
        profile-separator: '/'
        application-name: 'efgs'
        default-context: ''
        profiles: efgs
