---
logging:
  level:
    org:
      springframework: off
    root: off

spring:
  main:
    banner-mode: off
  flyway:
    enabled: true
    locations: classpath:db/migration/postgres
    jpa:
      hibernate:
        ddl-auto: validate
